# ── Fiomio — environment variables ───────────────────────────
# Copy this file to `.env.local` and fill in what you need.

# Waitlist destination (optional but recommended for production).
# When set, /api/waitlist forwards each signup as JSON to this URL.
# Works with anything that accepts a POST JSON webhook:
#   • Google Sheet  → Apps Script web-app URL
#   • Zapier / Make → "Catch Hook" URL
#   • Formspree     → https://formspree.io/f/xxxxxxx
#   • Resend, Beehiiv, ConvertKit, etc.
# If left empty, signups are stored in ./data/waitlist.json in local dev
# (not persisted on serverless hosting — set this before launch).
WAITLIST_WEBHOOK_URL=

# Brevo (preferred: free, collects + stores contacts; send from Brevo or
# export to any other ESP later — no lock-in). Brevo dashboard →
# Settings → SMTP & API → API Keys → create a key (server-side SECRET,
# no NEXT_PUBLIC_). BREVO_LIST_ID is optional: the numeric id of the list
# new signups land in (Contacts → Lists → the list's "ID" column). When
# BREVO_API_KEY is set, /api/waitlist adds each signup as a Brevo contact.
BREVO_API_KEY=
BREVO_LIST_ID=

# Sovrn Commerce (ex-Skimlinks) auto-affiliation publisher id.
# Once your site is approved, grab the id from the Sovrn dashboard script
# ( https://s.skimresources.com/js/<THIS_ID>.skimlinks.js ) and set it here.
# When set, every outbound merchant link is auto-converted to an affiliate
# link. Leave empty until approved. Must be NEXT_PUBLIC_ (loads client-side).
NEXT_PUBLIC_SOVRN_ID=

# Direct affiliate tags for KEY retailers (higher commission than Sovrn).
# Optional: set once approved by each program; otherwise Sovrn handles the
# link. The query-param format per program lives in src/lib/affiliates.ts —
# adjust there to match each program's exact instructions.
NEXT_PUBLIC_AFF_AMAZON=      # Amazon Associates store id, e.g. fiomio-21
NEXT_PUBLIC_AFF_YESSTYLE=
NEXT_PUBLIC_AFF_STYLEVANA=
NEXT_PUBLIC_AFF_IHERB=

# Anthropic (Claude) — powers the personalised AI skin note in the diagnostic.
# Set in .env.local for dev, and in Vercel project settings for production.
ANTHROPIC_API_KEY=

# --- Data flywheel (anonymous diagnostic storage) ---
# Reuses the Supabase project (see SUPABASE_URL / SUPABASE_SERVICE_KEY above
# if already set). Create a "diagnostics" table; rows carry NO email / NO IP.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
# Optional generic fallback if you don't use Supabase:
DIAGNOSTIC_WEBHOOK_URL=

# --- Seasonal newsletter (retention) ---
# Vercel Cron auth (Vercel sends it as `Authorization: Bearer <CRON_SECRET>`).
CRON_SECRET=
# Verified Brevo sender for the seasonal emails.
NEWSLETTER_FROM=hello@fiomio.io
# In Brevo, create contact attributes: CITY (text), LAT (number), LON (number),
# LANG (text), LAST_SEASON (text). The newsletter cron uses LAST_SEASON to avoid
# emailing the same season twice.
